name: local_stt_lab
description: Local speech model evaluation client
publish_to: none
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  record: ^6.0.0
  web_socket_channel: ^3.0.1
  http: ^1.2.2
  file_picker: ^10.3.3
  just_audio: ^0.10.5
dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
